# Test Image Fixtures

This directory contains test images for E2E tests.

## Test Images
- `valid-photo.jpg` - Valid JPEG image (< 5MB) for upload tests
- `large-photo.jpg` - Large image (> 5MB) for validation tests
- `invalid-file.txt` - Non-image file for type validation tests
